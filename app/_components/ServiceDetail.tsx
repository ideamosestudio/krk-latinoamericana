import { InnerHero, InnerShell, base } from "./InnerShell";

export type ProductItem = { title: string; en: string; es: string; image: string };

export function ServiceDetail({ title, en, es, image, introTitle = "Ingeniería para", introAccent = "operaciones exigentes.", items = [] }: { title: string; en: string; es: string; image: string; introTitle?: string; introAccent?: string; items?: ProductItem[] }) {
  return <InnerShell><InnerHero eyebrow="PRODUCTOS Y SERVICIOS" title={title} accent="by KRK." image={image} description="Ingeniería, fabricación y ejecución integradas para el movimiento confiable de materiales." /><section className="detail-intro section" id="contenido"><div className="container detail-editorial"><aside><div className="eyebrow"><span /> KRK / CAPABILITIES</div><p>Soluciones desarrolladas para cada condición de operación.</p></aside><div><h2>{introTitle}<br /><em>{introAccent}</em></h2><div className="detail-language-grid"><div><small>EN / OVERVIEW</small><p className="english">{en}</p></div><div><small>ES / DESCRIPCIÓN</small><p>{es}</p></div></div><div className="detail-actions"><a className="detail-cta dark" href={`${base}/#contacto`}>Conversar con ingeniería <i>↗</i></a><a className="detail-secondary" href={`${base}/servicios/`}><i>←</i> Ver todos los servicios</a></div></div></div></section>{items.length > 0 && <section className="product-catalog"><div className="container catalog-heading"><div className="eyebrow light"><span /> EQUIPOS</div><h2>Soluciones para un flujo<br /><em>continuo y confiable.</em></h2></div><div className="container">{items.map((item, index) => <article className="product-row" key={item.title}><div className="product-number">KRK / {String(index + 1).padStart(2, "0")}</div><div className="product-image"><img src={`${base}/images/${item.image}`} alt={item.title} /></div><div className="product-text"><small>EQUIPMENT / {String(index + 1).padStart(2, "0")}</small><h2>{item.title}</h2><div className="product-copy-grid"><div><b>EN</b><p className="english">{item.en}</p></div><div><b>ES</b><p>{item.es}</p></div></div></div></article>)}</div></section>}</InnerShell>;
}
